#!/usr/bin/env php
<?php

require __DIR__.'/../vendor/autoload.php';

use ConsoleLib\Application;
use Examples\DemoCommand;
use Examples\MakeMeTeaCommand;

$app = new Application();
$app->registerCommand(new DemoCommand());
$app->registerCommand(new MakeMeTeaCommand());
$app->run($argv);